# Local wrangler dev only — production uses `wrangler secret put`
# AUTH_PEPPER is install/hash bootstrap, not the owner login passtoken.
AUTH_PEPPER=replace_me_with_a_long_random_string
# CF_ACCOUNT_ID=
# CF_API_TOKEN=
